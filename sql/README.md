# UUID-Base32 encoding in SQL

SQL functions to convert UUIDs to and from base 32 directly in your database.
Fully compatible with the UUID class.

Just copy paste the SQL you want to use into one of your database migrations.
